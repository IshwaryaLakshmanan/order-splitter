# Application
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# Database (PostgreSQL - optional)
USE_POSTGRES=false
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=order_splitter
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_POOL_SIZE=10
DATABASE_POOL_IDLE_TIMEOUT=30000

# Authentication
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRATION=86400

# CORS
CORS_ORIGINS=http://localhost:3000,https://example.com

# Order Splitting
SHARE_DECIMAL_PRECISION=3
FIXED_PRICE=100

# NOTE: Kafka Configuration (optional)
# Kafka can be enabled for advanced messaging capabilities.
# For real multi-service trading systems (order execution, risk management, etc.),
# Kafka can be integrated for event-driven architecture and asynchronous processing.